query GetNetworksByLocationId($id: ID!) {
    locationById(id: $id){
        networks{
            edges {
                node {
                    ref
                }
            }
        }
    }
}